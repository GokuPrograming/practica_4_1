import 'package:flutter/material.dart';
import 'package:practica_4_1/home_screen2.dart';

void main() {
  runApp(HomeScreenv2());
}

/*class MyApp extends StatelessWidget {
  const MyApp({Key? key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      debugShowCheckedModeBanner:
          false, // Establecer en false para quitar la etiqueta de "DEBUG"

      home: Scaffold(
        appBar: AppBar(
          title: const Text('Mi Aplicación'),
        ),
        body: const Center(
          child: Text('Hola Mundo'),
        ),
      ),
    );
  }*/

